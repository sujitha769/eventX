services:
  mysql:
    image: mysql:8.4
    container_name: organizer-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: organizer_db
    ports:
      - "3307:3306"
    volumes:
      - organizer_mysql_data:/var/lib/mysql

volumes:
  organizer_mysql_data: